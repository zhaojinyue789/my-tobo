# 核心同步逻辑测试用例（修改后必跑）

每次修改 `sync.ts` 或数据流核心逻辑后，必须严格对照以下场景进行人工回归验证：

### 场景 A：同步窗口内的数据竞态（防丢数据）

1. **操作**：DevTools -> Network -> 切换为 `Slow 3G`。
2. **动作**：点击“立即同步”后，在状态显示“同步中...”的 3 秒内，**立即新建一条待办 B**。
3. **预期结果**：
   - [ ] 待办 A 和 B 在 UI 上同时存在，未发生覆盖或消失。
   - [ ] 刷新页面后，A 和 B 依然保留。
   - [ ] 检查 `localStorage` 中的 `my-tobo.todos`，同时包含 A 和 B。
   - [ ] 另一端设备能够正常拉取到 A 和 B。
4. **失败判据**：B 在同步完成后神秘消失。

### 场景 B：网络超时与断网恢复（防卡死）

1. **操作**：DevTools -> Network -> 切换为 `Offline`。
2. **动作**：点击“立即同步”。
3. **预期结果**：
   - [ ] 15 秒内，UI 状态变为“网络超时/不可用，稍后重试”。
   - [ ] 按钮恢复可用状态，未永久卡在“同步中...”。
   - [ ] 切换回 `Online` 后，30 秒内自动触发同步并恢复正常。

### 场景 C：远端脏数据防御（防白屏崩溃）

1. **操作**：确认已配置同步。浏览器 / PWA 环境配置在 localStorage 的
   `my-tobo.sync.gistId` 与 `my-tobo.token` 两个键（旧键 `my-tobo.sync` 已废弃，
   仅启动迁移时读取一次，运行中写它无效）；桌面端 Token 在 Store 文件
   （`%APPDATA%\com.mytobo.desktop\my-tobo.json`）。
   手动将 Gist 中的 JSON 内容改坏（例如删除某个 todo 的 `id` 字段或把 `text` 改为数字）。
2. **动作**：点击“立即同步”拉取脏数据。
3. **预期结果**：
   - [ ] 页面未发生白屏崩溃。
   - [ ] 脏数据条目被自动丢弃，控制台打印 `error` 日志。
   - [ ] 正常的 Todo 条目依然能够正常显示和同步。

### 场景 D：断网重连后的数据一致性

1. **操作**：离线状态下，在设备 A 和 B 上分别新增、修改、删除不同的待办。
2. **动作**：恢复设备 A 的网络，等待其同步完成；再恢复设备 B 的网络。
3. **预期结果**：
   - [ ] 最终设备 A 和 B 的数据完全一致。
   - [ ] 没有任何待办发生永久性丢失或“复活”（墓碑机制生效）。

---

# 数据格式 v2 功能用例：分类 / 截止日期 / 通知（阶段 1–5 产出）

> 状态标记：**[已验证]** = 已有前几阶段实际观测数据（标注来源）；**[待实测]** = 需在 Tauri 真实环境人工执行；**[说明性]** = 回归契约 / 设计说明，无可执行断言。
> 各组成员的归属层级见组标题括号（本地 / 同步 / UI / 通知）。`today` 以运行当日为准（已验证数据采集日 2026-09-19）。
> 涉及键：`my-tobo.todos`、迁移标记 `my-tobo.migrated.category_due_date`、Gist 文件 `my-tobo.json`。

## T-迁移幂等（本地）

### M1 首次加载执行迁移 [已验证，阶段3 S1｜本地]
- 前置：无迁移标记；`my-tobo.todos` = 3 条（纯 v1 条目 / 带 `category:" 工作 "`、`dueDate:"2026-09-01"` 条目 / 远古墓碑）。
- 操作：启动（loadTodos）。
- 期望：返回 3 条；todos 键写盘恰 1 次；标记写 1 次且值为 `"1"`；`" 工作 "`→`"工作"`，`"2026-09-01"` 逐字保留。（实测：todos写=1、标记写=1、标记值=`"1"`）

### M2 二次加载不重复写回，migrate 不被跳过 [已验证，阶段3 S2｜本地]
- 前置：M1 之后再次启动；另注入 `category:" bad "` 脏值。
- 操作：启动（loadTodos）。
- 期望：写盘 0 次、标记写 0 次；脏值仍被清洗为 `"bad"`。（实测：0/0，清洗为 `"bad"`）

### M3 迁移标记写入失败 → 下次重试 [已验证，阶段3 S3｜本地]
- 前置：无标记；setItem 对标记键抛 QuotaExceededError。
- 操作：启动；解除故障后再启动。
- 期望：首次返回条目不阻塞、写回成功、标记缺失；解除后一次调用内置位标记。（实测：2 条完整返回→重试后标记=`"1"`）

### M4 全部写入失败（配额满）→ 数据不丢 [已验证，阶段3 S4｜本地]
- 前置：setItem 对所有键抛 QuotaExceededError。
- 操作：启动；解除后再启动。
- 期望：条目完整返回、不崩；恢复后一次重试完成写回+置位。（实测：1 条完整、saveTodos 内部报错被捕获、重试后 todos写=1、标记=`"1"`）

### M5 墓碑条目同样补齐新字段 [已验证，阶段6 补测｜本地]
- 前置：仅 1 条 1 天龄墓碑（无 category/dueDate/notified）。
- 操作：启动（loadTodos）。
- 期望：返回 1 条；`deletedAt` 逐字保留；category=undefined、dueDate=undefined、notified=false；墓碑不被清除（<30 天）。（实测：全部成立）

## T-云端格式（同步）

### V1 无 version 字段 [已验证，阶段2 (a)｜同步]
- 前置：`my-tobo.json` 内容 `{ todos:[有效条目] }`（无 version）。
- 操作：拉取解析（parseRemoteTodos）。
- 期望：解析出 1 条有效条目。（实测 count=1）

### V2 version:1 [已验证，阶段2 (b)｜同步]
- 前置：`{ version:1, todos:[有效条目] }`。
- 期望：解析出 1 条。（实测 count=1）

### V3 version:2 含新字段 [已验证，阶段2 (c)｜同步]
- 前置：`{ version:2, todos:[ t1(category:" 工作 ", dueDate:"2026-09-01"), t2(category:123, dueDate:"2026/09/01") ] }`。
- 期望：2 条全保留；t1 → `"工作"`/`"2026-09-01"`；t2 → category/dueDate 均 undefined、条目不丢。（实测成立）

### V4 version:3 未来版本 [已验证，阶段2 (d)｜同步]
- 前置：`{ version:3, todos:[有效条目] }`。
- 期望：解析出 1 条；console.warn 恰 1 条（含 `版本号未识别` 与 `3`）；error 0 条。（实测 count=1、warn=1、error=0）

### V5 todos 缺失 / 非数组 [已验证，阶段2 (e)｜同步]
- 前置：`{ version:2 }` 与 `{ version:2, todos:"broken" }`。
- 期望：两者均返回 `[]`。（实测均 `[]`）

### V6 混入脏条目单条丢弃 [说明性，回归契约｜同步]
- 契约：text 缺失 / 类型错误的条目被丢弃并 console.error，其余条目不受影响；新字段（category/dueDate/notified）永不作为整条丢弃依据（阶段2 实测 error=0；脏条目路径由上方「场景 C」覆盖）。

## T-筛选与分类（UI）

### F1 交叉筛选矩阵 [已验证，阶段4 期望矩阵｜UI]
- 前置：4 条——工作A（未完成，dueDate=昨天）、工作B（已完成）、生活C（未完成）、未分类D（无分类）。
- 操作：遍历 分类（全部/工作/生活/未分类）× 状态（全部/未完成/已完成）。
- 期望（计数|成员）：全部 4|A,B,C/D → 3|A,C,D → 1|B；工作 2|A,B → 1|A → 1|B；生活 1|C → 1|C → 0|∅；未分类 1|D → 1|D → 0|∅。

### F2 已删条目不进候选集与统计 [已验证，阶段4｜UI]
- 前置：任一条目打墓碑。
- 期望：分类下拉候选与「剩余 N 项」计数均不含它（filterTodos / categoryOptions / 统计均经 isDeleted 过滤）。

### F3 分类删光自动消失 [待实测｜UI]
- 前置：仅 1 条「生活」；视图停在「生活」筛选。
- 操作：删除该条。
- 期望：分类下拉中「生活」消失；视图回退「全部」；计数正确。

### F4 新建分类规则 [待实测｜UI]
- 操作：依次输入空串、重复的「工作」、`" 工作 "`、新名称「旅行」。
- 期望：空值拒绝并提示；重复拒绝并提示；`" 工作 "` 归并为「工作」；「旅行」添加后立即出现在分类筛选下拉与新建下拉；无条目引用时仅内存存在，重启消失且无数据风险。

### F5 筛选零写盘 [待实测｜UI]
- 操作：快速连切分类/状态筛选 ≥10 次。
- 期望：saveTodos 调用 0 次（筛选 handler 只改视图态 + rAF render，不触碰 persist 链）；列表渲染始终正确。

### F6 XSS 纯文本 [待实测｜UI]
- 前置：分类名/文本含 `<script>`、`&`、`"`。
- 期望：以纯文本显示、无脚本执行、页面无异常。（代码事实：ui.ts/main.ts 0 处 innerHTML，15 处 textContent）

## T-截止日期与过期（本地/UI）

### D1 过期边界 [已验证，阶段1/4｜本地]
- today=2026-09-19 实测：`2026-09-18` 未完成未删 → true；`2026-09-19`（当天）→ false；`2026-09-20` → false。

### D2 永不过期三形态 [已验证，阶段1｜本地]
- 实测：completed=true 且日期过去 → false；带墓碑且日期过去 → false；无 dueDate → false。

### D3 非法日期输入 [待实测｜UI]
- 操作：日期框手动敲 `2026/9/1`。
- 期望：字段清空、条目不丢、无报错弹窗；若清掉的是原有过期日期，updatedAt bump 一次。

### D4 格式校验已知限制 [已验证，阶段1｜本地]
- 实测：`isValidDueDate("2026-13-99") === true`（只验格式不验历法，对应 CHANGELOG Known limitations）。

### D5 改日期 bump、渲染赋值不 bump [待实测｜本地]
- 操作：改一条日期为合法值；随后仅切换筛选/重进页面。
- 期望：改动后该条 updatedAt = 新的 Date.now()；纯渲染路径下 updatedAt 保持不变。

## T-同步回归（同步）

### S1 断网改动不丢 [待实测｜同步]
- 操作：Offline 下改分类、改日期、新建条目；恢复网络等自动同步；第二端拉取。
- 期望：本端三处改动全部保留；两端数据一致；Gist 内容 `version:2` 且含新字段。

### S2 新旧版本互读（字段存活与 LWW 机制）[说明性，证明「全端同版本」纪律｜同步]
- 机制：LWW 按 updatedAt 整条覆盖。阶段2 逐段核实本仓库 v1 链路为 spread 透传（不剥新字段），故本仓库真实旧版互读时新字段**随条目存活**；「字段被清」风险仅在存在剥离型旧版（白名单重建式 sanitize）时成立——演示方法：临时把 sanitizeRemoteTodo 改为白名单重建 → v2 数据经其读并回写 → category/dueDate 从 Gist 消失。
- 结论：跨版本的真实风险是清洗规则不一致（v1 不清洗非法 category），故须全端同版本升级（CHANGELOG Known limitations）。

### S3 notified 跨设备语义 [说明性｜同步]
- 设计：标记 notified 时不 bump updatedAt → 另一端不会经 LWW 采用该标记 → 各端独立判定与通知；若另一端内容编辑（bump）覆盖该条，标记随条目丢失 → 该端下次触发重发一次。属可接受降级，非数据丢失。

## T-通知（通知）

### N1 启动扫描 [待实测｜通知]
- 前置：≥1 条「过期 + 未完成 + 未删除 + notified≠true」；系统通知已授权。
- 操作：冷启动。
- 期望：**当前实现为逐条通知**——N 条过期未通知 → 弹 N 条（标题 `待办已到期`）；0 条不弹；成功者 notified=true 且统一落盘 1 次。⚠ 原计划为「单条汇总（你有 N 条已过期）」，阶段5 按其规格落地为逐条——保留逐条还是改汇总（改一处循环即可）请拍板。

### N2 权限拒绝静默 [待实测｜通知]
- 操作：系统设置拒绝通知后冷启动。
- 期望：无通知、无报错弹窗、列表正常渲染；notified 不被置位（授权后可补发）。

### N3 已通知不重复 [待实测｜通知]
- 操作：N1 完成后再次冷启动。
- 期望：0 条通知（notified=true 的条目被跳过）。

### N4 行内改日期即发即标 [待实测｜通知]
- 操作：把一条未完成条目的日期改为昨天。
- 期望：立即弹 1 条；该条 notified=true；localStorage 落盘含 `"notified":true`。

### N5 文案 [待实测｜通知]
- 期望：有分类 → 正文 `分类名：文本 已到期`；无分类 → `文本 已到期`；标题恒为 `待办已到期`。

### N6 通知服务不可用 / dev 浏览器降级 [待实测｜通知]
- 操作：关闭系统通知服务；或 `npm run dev` 纯浏览器打开。
- 期望：不崩、无报错、列表/同步主流程不受影响；dev 浏览器下 ensurePermission 直接返回 false（`__TAURI_INTERNALS__` 不存在）。
