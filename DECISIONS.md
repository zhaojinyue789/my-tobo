# DECISIONS：夜间值守决策记录

> 格式：问题 → 选择 → 理由 → 可回退方式。无人值守期间的所有设计取舍按序追加。

## D1 显示方向与新项位置

- 问题：现状「最新在顶」（unshift + createdAt 降序），任务书 ⑤/⑦ 明确「新项 = 末项 + STEP」「新项默认排末尾」，两者冲突。
- 选择：按任务书执行——order 升序 = 自上而下，旧数据按 createdAt **升序**推导，新条目追加到列表末尾。
- 理由：任务书两处显式要求且其算术（末项+STEP）与之自洽；拖拽类待办应用惯例亦为追加到底部；无人值守协议规定按保守（=遵循明确任务书）执行。
- 可回退：revert 提交 `feat: order 字段与稳定排序`；或最小改动——把 `derivedOrderMap` 名次取反、`nextAppendOrder`/插入计算改为 `首项 − STEP` 方向（约 15 行）。

## D2 首次拖拽的写入范围

- 问题：首次拖拽只写被拖项的 order，还是全量物化所有现存条目？
- 选择：全量物化（按当前有效顺序写入 `(名次+1)×STEP`）。
- 理由：推导值依赖「全量列表名次」，跨设备墓碑集合不同会导致推导值分歧；全量物化后现存条目全部有显式值、新条目创建时也赋显式值，混合窗口基本消除，跨端显示一致性显著更好。一次性写入由 3 秒防抖合并为单次推送。
- 可回退：删除 `materializeOrders` 调用，仅保留被拖项写入（比较函数已兼容混合值）。

## D3 撤销「新建」= 打墓碑

- 问题：undo(create) 应物理删除还是打墓碑？
- 选择：打墓碑（与手动删除同路径）。
- 理由：物理删除后远端仍存有该条目，下次合并会按 LWW 复活，撤销就「不生效」；墓碑是本仓库既有的删除语义。
- 可回退：无需要回退的场景；如坚持物理删除须同时改造同步合并，不建议。

## D4 撤销/重做应用时一律 bump updatedAt

- 问题：inverse「精确还原」是否包含还原历史 updatedAt？
- 选择：业务字段（order/deletedAt/category/dueDate/completed/text）精确还原，`updatedAt` 一律刷新为当前时间。
- 理由：sync 层 LWW 以 updatedAt 整条裁决；若撤销沿用旧时间戳，远端较新的副本会把撤销结果改回去，撤销将无法同步。这是「精确还原」与「可同步」的唯一兼容解。
- 可回退：不适用（还原旧时间戳会导致撤销在双端场景失效）。

## D5 清除已完成 = 单条 clearAll 型命令

- 问题：任务书命令清单未含「清除已完成」，如何入栈？
- 选择：复用 `clearAll` 类型（批量打墓碑统称），targets 仅含被清除的已完成条目。
- 理由：语义同构（批量墓碑单命令、一次撤销全恢复），不新增类型；confirm 文案同步改为「可通过撤销恢复」以保持真实。
- 可回退：main.ts 中清除已完成改为不 push 命令（回到不可撤销行为）。

## D6 undo 栈持久化与损坏处理

- 问题：redo 栈是否持久化？存储损坏如何处理？
- 选择：`my-tobo.undo` 同时持久化 undo/redo 两条栈（结构 `{v:1, undo[], redo[]}`）；加载时逐条结构校验，非法条目剔除，整体损坏则空栈兜底。
- 理由：验收要求「刷新页面后栈仍在」，redo 一并保留体验完整；校验剔除比整体丢弃更保守（保留可用部分）。
- 可回退：仅持久化 undo（redo 改会话内存），删 saveUndoState 的 redo 字段即可。

## D7 失效命令的静默跳过

- 问题：命令引用的 id 不存在（墓碑已清理/远端删除）时怎么办？
- 选择：应用时逐 id 查找，找不到则跳过该 id，命令其余部分照常应用；栈不清空、不报错。
- 理由：符合「空栈/异常操作无副作用不报错」的精神；清空整个栈会连带丢掉仍有效的撤销能力。
- 可回退：改为「应用失败即清空栈」（更激进，不建议）。

## D8 快捷键范围

- 问题：是否追加 Ctrl+Y 等常见重做别名？输入框屏蔽范围？
- 选择：仅 Ctrl+Z / Ctrl+Shift+Z（Mac 用 Cmd）；屏蔽范围 = input/textarea/contentEditable（select 不屏蔽）。
- 理由：任务书只规定这两组；最小实现避免与浏览器原生行为冲突面扩大。
- 可回退：keydown 处理器中追加 `e.key.toLowerCase() === "y"` 分支即可。

## D9 rebalance 的触发与归属

- 问题：重整何时触发？拖拽引发的重整算独立命令还是并入 reorder？
- 选择：拖拽中「间距 < 256 或新序值与现有值相等」→ 内联重整并入当次 reorder 命令（一次撤销还原重整前顺序）；同步合并后检测到现存条目相等序值 → 调度独立 rebalance 命令，`queueMicrotask` 同一 tick 去抖。
- 理由：验收 4 要求一次撤销精确还原拖拽（含重整前顺序），必须是单命令；独立触发走独立命令便于撤销且不与拖拽耦合。
- 可回退：删掉合并后的 `scheduleRebalanceIfNeeded`（显示不受影响，仅序值密度不再自动归一）。

## D10 触摸拖拽激活方式

- 问题：触摸端何时进入拖拽态（立即跟随 / 长按激活）？
- 选择：长按 ≈250ms 激活；激活前的 touchmove 取消激活计时并放行为滚动。
- 理由：立即跟随会吞掉页面滚动手势，列表场景不可用；长按是移动端列表重排的通行做法，保守可靠。
- 可回退：调小/调大 `TOUCH_LONG_PRESS_MS` 常量，或改为「拖拽手柄」模式（需改 UI，不建议本夜做）。

## D11 撤销/重做按钮位置与形态

- 问题：工具栏按钮放哪、什么样式？
- 选择：放在复合筛选按钮右侧成组（30×30 图标按钮，样式沿用 sync-btn 的视觉语言），随 render 同步禁用态。
- 理由：与既有工具栏视觉一致、不与「新建分类」触发器抢右侧位置；触摸目标在移动端经 (pointer:coarse) 适当放大。
- 可回退：纯 UI 调整，移动 buildToolbar 中节点位置即可。
